package io.b306.fitune.api

import retrofit2.Call
import retrofit2.http.GET
import retrofit2.http.Path

interface MyInfo {
//    @GET("user/super/{userSeq}")
//    fun getMyInfo(@Path("userSeq") userId: Int) : Call<UserResponse>
}